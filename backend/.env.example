# MySQL connection string (adjust username/password/host/database)
DATABASE_URL=mysql+pymysql://root:password@localhost:3306/reservation_parking

# CORS origins for frontend (comma separated). Example for local Vite dev server:
CORS_ORIGINS=http://localhost:5173

# Auth / Admin
SECRET_KEY=change-this-in-production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
